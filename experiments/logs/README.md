Pre-registered hypotheses and empirical run records live here (protocol Phase 4).
One file per experiment: EXP_<ID>.md, written BEFORE the run, never edited after
the result is known -- corrections are appended, not applied in place.
