# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/
env/

# Editor / OS
.vscode/
.idea/
.DS_Store

# Claude Code local session state
.claude/

# Training artifacts.
# Keep the two checkpoints the README ships and references; ignore ad-hoc
# training outputs (the default --ckpt name, the quick-start scratch file, and
# any other *.pt a training run produces).
*.pt
!demo.pt
!prose.pt
snn_char_lm.pt
my_prose.pt

# Cached Project Gutenberg downloads for build_corpus.py (input.txt is the
# committed artifact; the cache is just to make a rebuild offline and instant)
.corpus_cache/

# Tier 6 experiment outputs (checkpoints + metrics CSVs)
runs/

# Tier 7 chat-campaign data (rebuildable via build_chat_corpus.py; ~400 MB)
corpus/
!spark.pt
